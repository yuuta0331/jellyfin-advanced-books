name: Temporary focal zoom branch cleanup

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cleanup-focal-zoom-branch.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Delete merged branch
        shell: bash
        run: |
          set -euo pipefail
          endpoint="repos/${GITHUB_REPOSITORY}/git/refs/heads/feature/focal-zoom-touch-pan-compact-bar"
          if gh api "$endpoint" >/dev/null 2>&1; then
            gh api --method DELETE "$endpoint"
            echo "Deleted feature/focal-zoom-touch-pan-compact-bar"
          else
            echo "Branch already absent"
          fi
