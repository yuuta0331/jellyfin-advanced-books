name: Temporary stale branch cleanup

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cleanup-stale-reader-branch.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Delete stale merged branch
        shell: bash
        run: |
          set -euo pipefail
          endpoint="repos/${GITHUB_REPOSITORY}/git/refs/heads/feature/v0.15-reader-ux-language"
          if gh api "$endpoint" >/dev/null 2>&1; then
            gh api --method DELETE "$endpoint"
            echo "Deleted feature/v0.15-reader-ux-language"
          else
            echo "Branch already absent"
          fi
