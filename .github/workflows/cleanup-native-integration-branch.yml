name: Temporary native integration branch cleanup

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/cleanup-native-integration-branch.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Delete merged branch
        shell: bash
        run: |
          set -euo pipefail
          endpoint="repos/${GITHUB_REPOSITORY}/git/refs/heads/feature/native-reader-integration-history-icons"
          if gh api "$endpoint" >/dev/null 2>&1; then
            gh api --method DELETE "$endpoint"
            echo "Deleted feature/native-reader-integration-history-icons"
          else
            echo "Branch already absent"
          fi
